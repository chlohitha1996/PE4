package Java;

public class ReplaceCharacters {

        public String ReplaceCharacter(String string,char a,char b){
            System.out.println(string.replace(a,b)); //Using String Replace method to replace char a with char b
            return string.replace(a,b);

        }
}
